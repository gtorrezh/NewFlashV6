<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project1.uf1.dam.newflashv6">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".xmlpullparser.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoticiaVisualizacion.WebVisual" />
        <activity android:name=".categoria.ActividadDetalle" />
        <activity android:name=".BaseDeDatos.Favoritos" />
        <activity android:name=".categoria.ListCategorias" />
        <activity android:name=".BaseDeDatos.MainDBNewflash"></activity>
    </application>

</manifest>
